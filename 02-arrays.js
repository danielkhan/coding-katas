// Declare an array 'numberArray and store the numbers 1 to 10 in it

// Declare an array 'stringArray' and store the strings 'one', 'two', 'three' up to 'ten' in it

// Declare an array 'foxArray' and store the words contained in the sentence 'The quick brown fox jumps over the lazy dog' in it

// Loop through numberArray and log out each value

// Loop through stringArray and log out each value

// Loop through foxArray and log out each value

// Log the value of numberArray at index 3

// Log the value of stringArray at index 7

// Log out the string 'seven' in stringArray

// Log out the number '3' in numberArray

// Change 'fox' to cat in foxArray

// Loop through foxArray and log out each value

// Add a '!' to the end of foxArray 

// Loop through foxArray and log out each value

// Write a function 'changeFunction' that accepts an array, an index and a value, stores the passed variable at the given index and returns the array

// Use changeFunction to change 'fox' back to 'cat' in foxArray and log out the result